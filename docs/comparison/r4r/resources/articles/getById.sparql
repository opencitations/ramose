PREFIX dcterms: <http://purl.org/dc/terms/>
PREFIX prism: <http://prismstandard.org/namespaces/basic/2.0/>

SELECT (?id AS ?omid) ?title ?pubdate
WHERE {
  ?id dcterms:title ?title .
  OPTIONAL { ?id prism:publicationDate ?pubdate }
}
