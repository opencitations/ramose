PREFIX pro: <http://purl.org/spar/pro/>
PREFIX foaf: <http://xmlns.com/foaf/0.1/>

SELECT ?agent (CONCAT(?given, " ", ?family) AS ?name)
WHERE {
  ?id pro:isDocumentContextFor ?role .
  ?role pro:isHeldBy ?agent .
  ?agent foaf:familyName ?family ;
         foaf:givenName ?given .
}
