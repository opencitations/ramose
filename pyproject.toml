[project]
name = "ramose"
version = "2.0.0"
description = "Restful API Manager Over SPARQL Endpoints (RAMOSE) is an application that allows agile development and publication of documented RESTful APIs for querying SPARQL endpoints, according to a particular specification document."
authors = [
    { name = "essepuntato", email = "essepuntato@gmail.com" },
    { name = "marilena", email = "marilena.daquino2@unibo.it" },
    { name = "dbrembilla", email = "davide.brembilla98@gmail.com" },
    { name = "Ivan Heibi", email = "ivanhb.ita@gmail.com" },
    { name = "Arcangelo Massari", email = "arcangelo.massari@unibo.it" },
]
license = { text = "ISC" }
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "isodate>=0.6.1",
    "markdown>=3.6",
    "python-dateutil>=2.8.2",
    "rdflib>=6.1.1",
    "requests>=2.22.0",
]

[project.urls]
Repository = "https://github.com/opencitations/ramose/"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.coverage.run]
source = ["ramose"]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]

[dependency-groups]
dev = [
    "coverage>=7.13.0",
    "genbadge[coverage]>=1.1.3",
    "pyright>=1.1.408",
    "pytest>=9.0.2",
    "reuse>=6.2.0",
]
